# 更新日志

## v0.1.1 · 2026-09-11

### 修复

- **修复历史页关闭大图后会误跳到「设置」页的问题。**
  手机上「关闭」按钮的位置正好压在底部导航的「设置」标签上方。点关闭的瞬间大图已经消失，但画面重新渲染回网格需要一点时间；在这段"眼睛看到的是大图、浏览器已经判定大图不在"的空隙里再点一下，点击会直接穿透到下面的「设置」标签，于是大图消失的同时页面被切走。
  现在的处理：大图关闭后的一小段极短时间内，落在底部导航上的点击会被忽略；这段时间是从"画面真正回到网格之后"才开始计算的，所以手机渲染再慢也不会漏掉。

- **大图改为立即关闭。** 原来关闭动作会延后一帧执行，存在"已经隐藏、但状态上还开着"的中间态。现在关闭立即生效，卸图与清理这些耗时的操作仍延后一帧，关闭原图的流畅度保持不变。

- **大图查看器底部按钮增加按下反馈。** 删除、复制、收藏、关闭四个按钮按下时立刻缩小并变亮，点下去马上有视觉响应，不必再补第二下。

### 说明

- 本次只修改手机端页面文件，不涉及服务端逻辑。
- 更新后请在手机浏览器刷新一次页面即可生效，**不需要重启 ComfyUI**。

## v0.1.0 · 首个公开版本

- 手机浏览器通过 Tailscale 地址或 Cloudflare 临时公网链接远程控制电脑上的 ComfyUI，无需账号、密码或配对流程。
- 不注册任何节点，不改变原有工作流和连线；手机端参数表单由电脑端画布自动同步生成。
- 电脑端 ComfyUI 左侧快捷栏提供「手机远程」面板，统一管理两种连接方式，并在 Tailscale 在线时自动停用临时公网。
- 手机端五个页面：生成（文生图 / 图生图、多模型队列、提示词预设）、队列、历史（批次浏览、收藏、全屏查看器、滑动切换、缩放）、设置（与电脑端双向同步）。
- 提示词预设管理器：内置标签词库、互斥与跳过规则、类别锁定与忽略、桌面端标签管理页。
- 历史记录持久化、缩略图加速、图片彻底删除、收藏与工作流备份。
