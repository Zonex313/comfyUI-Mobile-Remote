# 更新日志

## v0.2.2 · 2026-09-15

- 电脑端「手机远程」面板新增「导入工作流」卡片：从磁盘上已保存的工作流里挑着导入，导入后常驻手机端，电脑端全关也能用。
- 没导入的工作流还是老规矩——电脑端打开着，手机端才读得到。
- 手机端工作流下拉框分成「常驻 · 电脑端不开也能用」和「电脑端打开中」两组。
- 导入后如果又在电脑端打开该工作流并做了修改，手机端那份会跟着更新；也可以手动「刷新」回磁盘上的版本。
- 导入列表按 workflows 目录的真实层级铺开（和电脑端工作流浏览器里看到的一样），支持按名字或目录搜索，还有「展开全部 / 收起全部」。
- 工作流的名称和地址一律全文显示、不省略号，操作按钮单独占一行，不再挤压文字。
- 导入页顶部有「已导入的工作流」卡片：按「常驻 · 电脑端不开也能用」和「仅电脑端打开时可见」两组列出已经进来的工作流，可直接刷新、设为常驻、取消常驻或删除。
- 可随时取消常驻、或从手机端删除（都不会动磁盘上的工作流文件）。
- 「加入随机标签节点」和「导入工作流」合并成一张卡片，两个按钮并排一行，卡片上不再有文字；工作流相关说明都移进「导入工作流」子页。
- 子页（标签管理 / 导入工作流）右上角只留「返回」，「检查更新」不再显示。
- 删掉冗余的「刷新连接状态」按钮：面板可见时本来就每 3 秒自动读一次连接状态。
- 导入页的操作提示（已设为常驻 / 已取消常驻 / 删除等）改成 3 秒后自动回到默认文字，不再一直挂着。
- 手机端「设置 → 重新读取工作流」按钮删除：和右上角「刷新」完全重复，那个刷新本来就会重新读取工作流列表。
- 临时公网卡片的提示文案改短：Tailscale 生效时自动关闭 Cloudflare 的提示改为「Tailscale生效中，自动关闭 Cloudflare」。
- 修复电脑端「手机远程」标题右下角的版本号在进过「标签管理」后会消失的问题。

## v0.2.1 · 2026-09-11

- 自动更新更稳：备份只保留最近 2 份，不再无限占用磁盘。
- 自动更新会清理新版本里已删除的旧文件（个人数据与运行产物不受影响）。
- 更新前会核对版本号：若远端在你确认后发生变化，会提示重新检查，不会更新成不是你确认的那个版本。
- 电脑端「手机远程」标题旁显示当前插件版本号。

## v0.2.0 · 2026-09-11

- 电脑端新增节点「CLIP文本编码丨随机标签」：平替 CLIP 文本编码，带标签模式开关。关闭时等同普通文本编码；开启后按手机端同一套词库与互斥规则随机组合标签，节点上可直接锁定、忽略、复制提示词。
- 手机远程面板新增「加入随机标签节点」按钮，一键把该节点放到画布中央。
- 手机远程面板新增更新检测：「检查更新」发现新版会变成高亮的「立即更新」，点一下自动下载并覆盖，任何一步出错都会自动回滚。
- 手机端工作流列表只显示电脑端当前打开着的工作流，关闭即消失；未保存的工作流不再同步。
- 手机端参数分组收敛：提示词、模型、图像尺寸留在外面，其余全部收进「高级参数」，并固定了前几项顺序。

## v0.1.2 · 2026-09-11

- 修复关闭大图后要等一下才生效的问题。
- 修复关闭按钮偶尔需要点两次才响应的问题。
- 关闭大图后立刻打开另一张图时，新图不会被误关。

## v0.1.1 · 2026-09-11

- 修复历史页关闭大图后误跳到「设置」页的问题。
- 大图查看器底部按钮增加按下反馈。

## v0.1.0 · 首个公开版本

- 手机浏览器通过 Tailscale 地址或 Cloudflare 临时公网链接远程控制电脑上的 ComfyUI。
- 不注册任何节点，不改动原有工作流和连线。
- 电脑端 ComfyUI 左侧快捷栏提供「手机远程」面板，统一管理两种连接方式。
- 手机端页面：生成、队列、历史（批次浏览、收藏、全屏查看器）、设置。
- 提示词预设管理器：内置标签词库、互斥与跳过规则、类别锁定与忽略。
- 历史记录持久化、缩略图加速、图片彻底删除、收藏与工作流备份。
